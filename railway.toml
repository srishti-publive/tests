[build]
builder = "dockerfile"

[deploy]
releaseCommand = "python manage.py migrate"
startCommand = "gunicorn publive_mcp.wsgi -w 1 --threads 50 -b 0.0.0.0:$PORT --timeout 60"
healthcheckPath = "/auth/status"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
