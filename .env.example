DJANGO_SECRET_KEY=change-me-to-a-long-random-string
BASE_URL=http://localhost:8000

DJANGO_ENV=local

# DATABASE_URL is optional locally (defaults to SQLite). All cross-worker state
# (SSE sessions, message queues, session stats, rate limits) is database-backed —

NEW_RELIC_LICENSE_KEY=
NEW_RELIC_APP_NAME=Publive MCP
NEW_RELIC_USER_KEY=
SERVER_VERSION=1.0.0
